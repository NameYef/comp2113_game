#include "player.h"
#include <iostream>

using namespace std;

void Player::setup() {
    // placeholder setup
    string a;
    cout << "press enter to finish player setup" << endl;
    getline(cin, a);
    return;
}