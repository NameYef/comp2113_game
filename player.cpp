#include "player.h"
#include <iostream>
#include <string>
using namespace std;

void Player::setup() {
    // placeholder setup
    string a;
    cout << "press enter to finish player setup" << endl;
    getline(cin, a);
    return;
}